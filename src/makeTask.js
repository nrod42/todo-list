function Task (title, description, dueDate, importance, completionStatus) {
    this.title = title;
    this.description = description;
    this.dueDate = dueDate;
    this.importance = importance;
    this.completionStatus = completionStatus;
}

export default Task;